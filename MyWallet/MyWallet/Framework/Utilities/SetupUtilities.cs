//using Microsoft.AppCenter;
//using Microsoft.AppCenter.Analytics;
//using Microsoft.AppCenter.Crashes;
//using Microsoft.AppCenter.Push;

namespace MyWallet.Framework.Extensions
{
    public static class SetupUtilities
    {
        public static void SetupPushNotifications(App app, string key)
        {
            //if (!AppCenter.Configured)
            //{
            //    Push.PushNotificationReceived += (sender, e) => app.HandlePushNotification(e);
            //}
            //AppCenter.Start(key, typeof(Push), typeof(Analytics), typeof(Crashes));
        }    
    }
}
